name: setup
description: setup
runs:
  using: composite
  steps:
    - uses: actions/setup-node@v7
      with:
        cache: yarn
        registry-url: 'https://registry.npmjs.org'
    - run: yarn
      shell: bash
